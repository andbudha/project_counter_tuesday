
export type MaxValStateType = number;

type MaxValAcType = any;
export const maxValReducer = (state: MaxValStateType, action: MaxValAcType) => {
    switch (action.type) {
        case 'XXX': {
            return state;
        } default : {
            return  state;
        }
    }
}